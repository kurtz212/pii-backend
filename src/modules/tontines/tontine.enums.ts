export enum TontineStatus {
  DRAFT = 'draft',
  ACTIVE = 'active',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum ContributionStatus {
  PENDING = 'pending',
  PAID = 'paid',
  MISSED = 'missed',
}