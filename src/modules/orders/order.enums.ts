export enum PaymentMethod {
  CASH = 'cash',
  TRANCHES = 'tranches',
}

export enum ReceptionMode {
  LIVRAISON = 'livraison',
  RENDEZ_VOUS = 'rendez_vous',
}

export enum OrderStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
}